{
  "name": "mcp-zoom-recordings",
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server for accessing and managing Zoom cloud recordings",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.24"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3",
    "@types/bun": "latest",
    "typescript": "^5.7"
  },
  "license": "MIT"
}
