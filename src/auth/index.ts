/**
 * Zoom authentication module
 *
 * Provides Server-to-Server OAuth authentication for the Zoom API.
 */

export { ZoomAuth } from "./zoom-auth";
export { ZoomAuthError, loadZoomConfig, type ZoomAuthConfig } from "./config";
